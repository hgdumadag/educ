# educ

Developer documentation for the combined education platform rebuild lives in `platform-rebuild-docs/`.

Start with:

- `platform-rebuild-docs/README.md`
- `platform-rebuild-docs/00-start-here.md`

